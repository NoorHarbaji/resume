import Raect from "react";

const ResumeTitle = ({ title }) => {
  return (
    <div className="adressDiv">
      <h2 className="headerLeftSide cv-item__title">{title}</h2>
    </div>
  );
};

export default ResumeTitle;
